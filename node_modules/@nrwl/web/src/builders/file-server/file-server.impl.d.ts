import { JsonObject } from '@angular-devkit/core';
import { TargetContext } from '@nrwl/devkit';
export interface FileServerOptions extends JsonObject {
    host: string;
    port: number;
    ssl: boolean;
    sslKey?: string;
    sslCert?: string;
    proxyUrl?: string;
    buildTarget: string;
    parallel: boolean;
    maxParallel: number;
    withDeps: boolean;
}
export default function (opts: FileServerOptions, context: TargetContext): Promise<unknown>;
